import { Button } from 'react-bootstrap'
import React from 'react'

function App() {
  return (
    <React.Fragment>
     <Button variant = "warning">Warning Buttons</Button>
      <Button variant = "info"><i className='fa fa-home'></i></Button>
    </React.Fragment>
  )
}

export default App
